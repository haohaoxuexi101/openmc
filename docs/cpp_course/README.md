# OpenMC 风格 C++ 课程代码

每节课都是一个可独立编译运行的示例程序，聚焦 OpenMC 在几何、粒子推进、缓存与性能计数等核心模式。编译示例（以 Lesson 1 为例）：

```bash
g++ -std=c++17 lesson01_surface_interface.cpp -o lesson01
./lesson01
```

其他课程类似，将文件名替换为对应的 `lessonXX` 即可。
