cmake_minimum_required(VERSION 3.10)

# 项目名称
project(OpenMC_Test)

# 指定 C++ 标准
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# 查找 HDF5 库
find_package(HDF5 REQUIRED COMPONENTS CXX)

# 添加可执行文件
add_executable(test_own test_lattice.cpp)

# 指定包含目录，以便找到 openmc 和 hdf5 的头文件
# include_directories(/home/ssn/ssn-file/mc_learn/openmc/include/)
include_directories(${HDF5_INCLUDE_DIRS})

# 链接库的路径，添加图片中显示的路径
# link_directories(/home/ssn/ssn-file/mc_learn/openmc/build/lib/)
# link_directories(${HDF5_LIBRARY_DIRS})

# 链接所需的库，包括 openmc、fmt 和 pugixml
# target_link_libraries(test_own PRIVATE ${HDF5_LIBRARIES} openmc fmt pugixml)
# target_link_libraries(test_own PRIVATE ${HDF5_LIBRARIES} openmc)
target_link_libraries(test_own PRIVATE openmc)
